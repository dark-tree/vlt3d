#pragma once

enum struct BillboardMode {
	ONE_AXIS,
	TWO_AXIS,
};

enum struct VerticalAlignment {
	TOP    = 0,
	CENTER = 1,
	BOTTOM = 2
};

enum struct HorizontalAlignment {
	LEFT   = 0,
	CENTER = 1,
	RIGHT  = 2
};